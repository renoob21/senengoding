---
title: Python List - Umum
author: Reno Iqbalsah
published: 23/03/2023
slug: dasar-dasar-python/python-list-umum
draft: false
description: Bagian ini memembahas tentang gambaran umum tipe data list dalam bahasa pemrograman Python
---
import CodeHL from "../../../components/PostsComps/CodeHL.astro"
import PostsNav from "../../../components/PostsComps/PostsNav.svelte"

Jika sejauh ini kita sudah melihat tentang tipe data _primitive_, selanjutnya kita akan masuk ke tipe-tipe data berbasis _object_, yang pertama
adalah `list`.  `list` adalah salah satu tipe data sekuen/urutan (_sequence_) dalam Python, atau biasa disebut juga dengan tipe data _array_.

Keunggulan `list` adalah dapat menyimpan banyak data dalam satu variabel. Karakteristik `list` antara lain:

- Bersifat urutan / sekuen
- Dapat diubah-ubah (_mutable_)
- Dapat diiterasi

Untuk membuat `list` baru, harus memasukkan seluruh data dalam kurung siku `[]` dan setiap data (biasa disebut item) dibatasi dengan koma `,`.
`list` dapat menampung tipe data apapun, bahkan bisa mencampurkan antara satu tipe data dengan tipe data lain dalam satu `list`.

<CodeHL
code={
`list_belanja = ["apel", "keju", "nangka", "nanas"]
list_random = [1, 2, "kucing", 1.0]

print(list_belanja) # output : ["apel", "keju", "nangka", "nanas"]
print(list_random) # output : [1, 2, "kucing", 1.0]`
}
lang="python"
/>

## _Indexing_ dan _Slicing_

Sebagaimana tipe data _sequence_, kita juga dapat melakukan _indexing_ dan _slicing_ sebagaimana kita lakukan pada [**_String_**](python-string-umum), hanya saja dalam 
string kita melakukan _indexing_ dan _slicing_ pada setiap karakter, pada `list` kita melakukannya pada setiap item (data yang dibatasi koma (`,`)).


**Contoh _indexing_:**
<CodeHL
code={
`list_belanja = ["apel", "keju", "nangka", "nanas"]

print(list_belanja[2]) # output : "nangka"
print(list_belanja[-1]) # output : "nanas"`
}
lang="python"
/>

**Contoh _slicing_:**
<CodeHL
code={
`list_belanja = ["apel", "keju", "nangka", "nanas"]

print(list_belanja[1:3]) # output : ["keju","nangka"]
print(list_belanja[3:]) # output : ["nagka", "nanas"]`
}
lang="python"
/>

Perlu diingat bahwa _slicing_ akan menghasilkan tipe data dari item itu sendiri, sedangkan _slicing_ akan menghasilkan `list` baru.

<CodeHL
code={
`list_belanja = ["apel", "keju", "nangka", "nanas"]

print(list_belanja[1], "=>", type(list_belanja[1])) 
# output : "keju" => str

print(list_belanja[3:], "=.", type(list_belanja[3:])) 
# output : ["nagka", "nanas"] => list`
}
lang="python"
/>

## Menghitung Jumlah Item dalam `list`

Kita dapat menggunakan fungsi `len()` untuk menghitung jumlah item dalam suatu `list`.

<CodeHL
code={
`list_belanja = ["apel", "keju", "nangka", "nanas"]


print(len(list_belanja)) # output : 4`
}
lang="python"
/>

<PostsNav
  next={{
    title: "Python List - Mengubah Item dalam List",
    slug: "python-list-mengubah-item-dalam-list"
  }}
  prev={{
    title: `Python Operator - Precedence (Hierarki Operator)`,
    slug: "python-operator-precedence-hierarki-operator"
  }}  
/>